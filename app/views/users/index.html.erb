<h1>Select Your Teacher</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Subject</th>
  </tr>

<% @users.each do |user| %>
  <tr>
    <td><%= link_to user.username, "/schools/" + user.id.to_s %></td>
    <td><%= user.school_id %></td>
  </tr>
<% end %>
</table>

<br />
